// this is the MYSQL configuration file it has the user paasword the name and the db name change them accordinly

module.exports = {
    host: 'localhost',
    user: 'root',
    password: 'root', // change accordinly
    database: 'Posts', // change accordinly
    insecureAuth: true,
}